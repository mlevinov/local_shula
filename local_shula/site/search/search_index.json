{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Shula Moodle Plugin","text":"<p>Welcome to the documentation for the <code>local_shula</code> Moodle plugin.</p>"},{"location":"#overview","title":"Overview","text":"<p>Shula is a multi-tenant LTI 1.3 Advantage tool designed for academic institutions. It provides an AI tutor integrated directly into Moodle courses. This local plugin acts as the bridge between the Moodle LMS and the Shula AI backend.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Real-time Webhooks (<code>Door A</code>): Proactively \"pushes\" content changes (creations, updates, deletions) to the Django backend using a Hierarchical v2.0 Ingestion model.</li> <li>Cascading Visibility: AI safeguards are driven by Moodle's visibility toggles. If a Section or Module is hidden, the plugin alerts the backend to instantly \"scrub\" or deactivate the associated AI vectors.</li> <li>Restore Storm Protection: Detects course restores and suppresses individual file events in favor of a single Bulk Sync task to prevent performance degradation.</li> <li>Decoupled Execution: Utilizes Moodle's <code>adhoc_tasks</code> triggered by Event Observers to ensure the Moodle UI remains fast and responsive.</li> </ul>"},{"location":"#version-information","title":"Version Information","text":"<ul> <li>Component: <code>local_shula</code></li> <li>Release: 1.2.5 (2026051900)</li> <li>Requirements: Moodle 4.1+</li> </ul>"},{"location":"#getting-started","title":"Getting Started","text":"<p>Navigate through the documentation to understand the inner workings of the plugin:</p> <ul> <li>Architecture: Understand the high-level design and ingestion workflow.</li> <li>Services: Learn how JSON payloads are constructed and sent securely.</li> <li>Tasks &amp; Webhooks: Explore how Moodle events are captured and processed in the background.</li> </ul>"},{"location":"architecture/","title":"Architecture &amp; Workflow","text":"<p>The <code>local_shula</code> plugin operates using a proactive \"push\" architecture (referred to as <code>Door A</code> in the project specs). It listens for native Moodle events and pushes contextual data to the Shula Django backend.</p>"},{"location":"architecture/#the-ingestion-workflow","title":"The Ingestion Workflow","text":"<p>The workflow is designed to be asynchronous to ensure Moodle's UI performance is never impacted by API latency.</p> <ol> <li> <p>Event Observation (<code>db/events.php</code> &amp; <code>classes/observer.php</code>)</p> <ul> <li>Moodle broadcasts events (e.g., <code>course_module_created</code>, <code>course_section_updated</code>).</li> <li>The <code>\\local_shula\\observer</code> catches these events.</li> <li>The observer checks if the Shula LTI tool is actively deployed in the given course.</li> <li>If active, it immediately queues an Adhoc Task and returns control to the user.</li> </ul> </li> <li> <p>Background Processing (<code>classes/task/</code>)</p> <ul> <li>Moodle's cron engine picks up the queued Adhoc Task (e.g., <code>send_webhook</code>, <code>send_section_webhook</code>, <code>bulk_sync</code>).</li> <li>The task runs asynchronously in the background.</li> </ul> </li> <li> <p>Payload Construction (<code>classes/service/payload_builder.php</code>)</p> <ul> <li>The task calls the <code>payload_builder</code> to construct a deeply nested JSON schema representing the Moodle objects.</li> <li>This schema includes availability rules, visibility toggles, and metadata for physical files and virtual content (like HTML pages or descriptions).</li> <li>It checks for AI exclusion tags (e.g., <code>no-shula</code>) to restrict specific content from being indexed.</li> </ul> </li> <li> <p>Secure Dispatch (<code>classes/service/client.php</code>)</p> <ul> <li>The constructed payload is passed to the API client.</li> <li>The client generates an HMAC-SHA256 signature using the configured webhook secret.</li> <li>The payload is POSTed to the Shula Django backend (<code>/api/v1/webhook/moodle/</code>).</li> </ul> </li> </ol>"},{"location":"architecture/#hierarchical-structure","title":"Hierarchical Structure","text":"<p>The JSON payloads strictly follow a hierarchical ingestion model to maintain Moodle's structural depth in the Shula AI vector database:</p> <p><code>Course -&gt; Section -&gt; Module -&gt; File/Content</code></p> <p>This ensures that the AI tutor always understands the exact context of the material it is referencing.</p>"},{"location":"testing/","title":"Testing Strategy","text":"<p>The <code>local_shula</code> plugin maintains a robust testing suite to ensure data integrity, security, and performance. </p>"},{"location":"testing/#phpunit-tests","title":"PHPUnit Tests","text":"<p>Moodle's native PHPUnit framework is used for unit and integration testing.</p>"},{"location":"testing/#security-validation-testspayload_builder_testphp","title":"Security Validation (<code>tests/payload_builder_test.php</code>)","text":"<p>This test suite focuses on the <code>payload_builder</code> and ensures that the security boundaries defined for AI ingestion are strictly enforced.</p> <ul> <li>Student Privacy Protection: Asserts that student-authored fileareas (like forum attachments and assignment submissions) are NEVER included in the ingestion allowlist.</li> <li>Allowlist Lock: A structural test that monitors the size of the allowed module list. If a developer adds support for a new module, this test fails, forcing a conscious security review of the new module's fileareas.</li> <li>Module Exclusion: Verifies that fully student-driven modules (Wikis, Glossaries, Databases) are explicitly excluded from physical file ingestion.</li> </ul>"},{"location":"testing/#running-tests","title":"Running Tests","text":"<p>To run the plugin's test suite, you must have a configured Moodle PHPUnit environment.</p> <pre><code># From your Moodle root directory\nvendor/bin/phpunit --group local_shula\n</code></pre>"},{"location":"testing/#manual-verification","title":"Manual Verification","text":"<p>For manual verification of outgoing payloads: 1. Enable Developer Debugging in Moodle. 2. Trigger an event (e.g., update a module description). 3. Check the Moodle cron logs or the Ad-Hoc task output in the site administration. The <code>client</code> service will print the full, signed JSON payload to the debug trace.</p>"},{"location":"services/client/","title":"API Client","text":"<p>Namespace: <code>\\local_shula\\service\\client</code></p> <p>The API Client handles the low-level HTTP communication between Moodle and the Shula Django backend. It ensures all outbound payloads are securely signed and delivered reliably.</p>"},{"location":"services/client/#security-authentication","title":"Security &amp; Authentication","text":"<p>Moodle webhooks are secured using an HMAC-SHA256 signature.</p> <ol> <li>The client takes the complete JSON payload.</li> <li>It generates a hash using the <code>shula_webhook_secret</code> configured in Moodle's Site Administration.</li> <li>The signature is attached to the outbound request via the <code>X-Moodle-Signature</code> HTTP header.</li> <li>The Django backend re-calculates the hash using its copy of the secret to verify the authenticity and integrity of the payload.</li> </ol>"},{"location":"services/client/#method-sendpayload","title":"Method: <code>send($payload)</code>","text":"<ul> <li>Injects the <code>issuer</code> (<code>$CFG-&gt;wwwroot</code>) into the payload so Django can identify the tenant.</li> <li>Uses Moodle's native <code>\\curl()</code> class to enforce SSRF protections.</li> <li>Enforces strict production connection timeouts (5s connect, 60s execution) and SSL verification.</li> <li>Throws a <code>\\moodle_exception</code> if the backend returns an HTTP status code &gt;= 400.</li> </ul>"},{"location":"services/client/#developer-debugging","title":"Developer Debugging","text":"<p>If Moodle's debugging is set to <code>DEBUG_DEVELOPER</code>, the client will intercept the request and print a beautifully formatted JSON output of the exact payload being dispatched directly into the Moodle debug trace log. This is invaluable for local testing.</p>"},{"location":"services/payload_builder/","title":"Payload Builder Service","text":"<p>Namespace: <code>\\local_shula\\service\\payload_builder</code></p> <p>The payload builder is the core data transformation engine of the plugin. It translates Moodle's complex internal data structures (Courses, Sections, Modules, and Files) into clean, deeply nested JSON schemas expected by the Shula Django backend.</p>"},{"location":"services/payload_builder/#key-methods","title":"Key Methods","text":""},{"location":"services/payload_builder/#build_course_treecourseid","title":"<code>build_course_tree($courseid)</code>","text":"<p>Generates the complete, nested JSON tree for a Bulk Sync event. *   Hierarchy: <code>Course -&gt; Section -&gt; Module -&gt; File/Content</code> *   Returns: A full array representing the entire course structure.</p>"},{"location":"services/payload_builder/#build_course_itemcourse","title":"<code>build_course_item($course)</code>","text":"<p>Builds the top-level course schema, extracting metadata like visibility, summary format, and language.</p>"},{"location":"services/payload_builder/#build_section_itemsection_info-modinfo-include_modules","title":"<code>build_section_item($section_info, $modinfo, $include_modules)</code>","text":"<p>Builds the schema for a specific course section. *   Extracts <code>unlock_date</code> from Moodle's complex availability rules. *   Can optionally nest child modules inside the section array.</p>"},{"location":"services/payload_builder/#build_module_itemcm","title":"<code>build_module_item($cm)</code>","text":"<p>Builds the schema for a course module natively. *   AI Restriction Check: Queries Moodle's Core Tag API to see if the module has an opt-out tag (e.g., <code>no-shula</code>). If tagged, the module's files are NOT included in the payload. *   Aggregates both physical files and virtual content.</p>"},{"location":"services/payload_builder/#build_virtual_items_from_cmcm-instance","title":"<code>build_virtual_items_from_cm($cm, $instance)</code>","text":"<p>Dynamically constructs items for native Moodle instances that don't have physical files attached. *   Handles <code>page</code>, <code>label</code>, and <code>url</code> modules. *   Generates a virtual text item for module introductions/descriptions.</p>"},{"location":"services/payload_builder/#build_file_items_from_cmcm","title":"<code>build_file_items_from_cm($cm)</code>","text":"<p>Queries Moodle's File Storage API to extract metadata and generate secure <code>pluginfile</code> URLs for physical files attached to a context. *   Teacher-Authored Security: Enforces a strict allowlist of teacher-only file areas (e.g., <code>mod_resource/content</code>, <code>mod_assign/introattachment</code>). Deliberately excludes student-authored attachments from modules like Wikis, Glossaries, and Forums to protect the integrity of the AI's knowledge base.</p>"},{"location":"services/payload_builder/#get_safe_teacher_fileareas","title":"<code>get_safe_teacher_fileareas()</code>","text":"<p>Returns the hardcoded mapping of Moodle components to their \"safe\" (teacher-authored) fileareas.  *   Security Principle: This acts as a firewall, ensuring that student privacy is maintained and the AI is not trained on potentially sensitive or irrelevant student submissions. *   Locked Allowlist: Any addition of new module support requires an explicit update to this list, accompanied by a security review.</p>"},{"location":"services/payload_builder/#security-privacy-validation","title":"Security &amp; Privacy Validation","text":"<p>To ensure that future updates don't accidentally leak student data, the plugin includes a dedicated PHPUnit test suite: <code>tests/payload_builder_test.php</code>.</p> <ul> <li>Risk Mitigation: The tests explicitly verify that high-risk areas (like <code>mod_forum</code> attachments or <code>mod_assign</code> submissions) are NOT in the allowlist.</li> <li>Drift Protection: An \"allowlist size lock\" test ensures that the number of supported modules doesn't grow silently, forcing developers to update tests when adding new features.</li> </ul>"},{"location":"services/payload_builder/#availability-parsing","title":"Availability Parsing","text":"<p>The class includes a private <code>extract_unlock_date($availability_json)</code> method that safely recursively parses Moodle's internal availability AST to find the strict \"Available from\" Unix timestamp.</p>"},{"location":"tasks/background_tasks/","title":"Background Tasks","text":"<p>Namespace: <code>\\local_shula\\task\\*</code></p> <p>To guarantee that Moodle remains fast for teachers and students, the plugin never blocks the UI to communicate with the Shula API. Instead, it queues background tasks using Moodle's Adhoc Task API. These tasks are picked up and executed asynchronously by the Moodle cron container.</p>"},{"location":"tasks/background_tasks/#bulk_sync","title":"<code>bulk_sync</code>","text":"<p>Trigger: LTI tool installation, course restoration, or manual trigger.</p> <ul> <li>This is the heaviest task.</li> <li>It calls <code>payload_builder::build_course_tree()</code> to construct a massive, deeply nested JSON representation of the entire course and every file inside it.</li> <li>Dispatches an <code>event_type: bulk_sync</code> to Django.</li> </ul>"},{"location":"tasks/background_tasks/#send_section_webhook","title":"<code>send_section_webhook</code>","text":"<p>Trigger: A section is created, renamed, moved, or its visibility is toggled.</p> <ul> <li>A lightweight, specialized task.</li> <li>Builds a payload containing the Course schema and just the specific Section schema (including nested modules).</li> <li>Significantly reduces payload size and database load compared to a full sync when only a section changes.</li> <li>Handles <code>section_deleted</code> events gracefully by passing only the IDs, as the DB records are already gone.</li> </ul>"},{"location":"tasks/background_tasks/#send_webhook","title":"<code>send_webhook</code>","text":"<p>Trigger: A module (file, page, url) is created, updated, or deleted.</p> <ul> <li>The workhorse task for day-to-day course edits.</li> <li>Builds a payload containing the Course, Section (without children), and the specific Module.</li> <li>Deletions: If an LTI tool is deleted, it relies on a snapshot passed by the observer, since the Moodle course record is wiped before the task runs.</li> </ul>"},{"location":"tasks/observer/","title":"Event Observer","text":"<p>Namespace: <code>\\local_shula\\observer</code></p> <p>The Event Observer acts as the central nervous system of the plugin. It listens for core Moodle events defined in <code>db/events.php</code> and decides if and how they should be routed to the Shula backend.</p>"},{"location":"tasks/observer/#the-observation-cycle","title":"The Observation Cycle","text":"<ol> <li>Event Fired: A user performs an action in Moodle (e.g., creating a module, hiding a section). Moodle fires the corresponding <code>\\core\\event\\*</code> class.</li> <li>Capture: The <code>observer</code> class catches the event.</li> <li>Restore Guard: <code>is_restoring()</code> checks if the event is part of a bulk course restore or plugin installation. If so, individual events are suppressed to prevent \"restore storms.\"</li> <li>Active Guard: <code>is_shula_active_in_course()</code> checks if the Shula LTI tool is present anywhere in the course. It uses a static cache to prevent redundant database queries during a single page load.</li> <li>Dispatch: If the guards pass, the observer packages the event IDs and queues the appropriate <code>adhoc_task</code>.</li> </ol>"},{"location":"tasks/observer/#handled-events","title":"Handled Events","text":""},{"location":"tasks/observer/#course-events","title":"Course Events","text":"<ul> <li><code>course_updated</code> -&gt; Queues <code>send_webhook</code> (Course Sync)</li> <li><code>course_deleted</code> -&gt; Generates a pre-deletion snapshot and queues <code>send_webhook</code> (LTI Tool Deleted)</li> <li><code>course_restored</code> -&gt; Suppresses file events and queues a single <code>bulk_sync</code> task.</li> </ul>"},{"location":"tasks/observer/#section-events","title":"Section Events","text":"<ul> <li><code>course_section_created</code></li> <li><code>course_section_updated</code></li> <li><code>course_section_deleted</code></li> <li>Action: Queues the lightweight <code>send_section_webhook</code> task.</li> </ul>"},{"location":"tasks/observer/#module-events","title":"Module Events","text":"<ul> <li><code>course_module_created</code></li> <li><code>course_module_deleted</code></li> <li><code>course_module_updated</code> (Crucial for detecting visibility toggles on specific files)</li> <li>Action: Queues <code>send_webhook</code> (File Created, File Deleted, File Updated).</li> <li>LTI Edge Case: If the module created/deleted is the Shula LTI tool itself, it triggers a full <code>bulk_sync</code> or teardown event.</li> </ul>"}]}